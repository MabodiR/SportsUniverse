# The latest @config-plugins/react-native-webrtc release still declares Expo 56
# as its peer while SportUniverse is on Expo 57. Keep npm installs aligned with
# the tested lockfile until the plugin publishes Expo 57 peer metadata.
legacy-peer-deps=true
