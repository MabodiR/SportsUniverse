const VERSION='su-pwa-v3';
const SHELL=['/feed','/explore','/opportunities','/images/logo/site.webmanifest','/images/logo/favicon-192x192.png','/images/logo/favicon-512x512.png'];
self.addEventListener('install',event=>event.waitUntil(caches.open(VERSION).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==VERSION).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET'||new URL(request.url).origin!==location.origin)return;const navigation=request.mode==='navigate';event.respondWith(fetch(request).then(response=>{if(response.ok&&(navigation||request.destination==='style'||request.destination==='script'||request.destination==='image'))caches.open(VERSION).then(cache=>cache.put(request,response.clone()));return response}).catch(async()=>await caches.match(request)||await caches.match(navigation?'/feed':request)))});
self.addEventListener('sync',event=>{if(event.tag==='sportuniverse-upload')event.waitUntil(self.clients.matchAll({type:'window',includeUncontrolled:true}).then(clients=>{clients.forEach(client=>client.postMessage({type:'RESUME_UPLOAD'}))}))});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(windows=>{const url=event.notification.data?.url||'/notifications';const existing=windows.find(window=>window.url.includes(url));return existing?existing.focus():clients.openWindow(url)}))});
